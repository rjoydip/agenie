# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: Check
      run: bunx oxfmt . && bunx oxlint . && bun turbo typecheck
      glob: "*.{ts,tsx,js,jsx,json,css,md}"
      runner: node
